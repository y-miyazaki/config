id: diagram-as-code-validation-contract-001
name: diagram-as-code-validation output contract sections
description: |
  Validate that diagram-as-code-validation returns mandatory validation sections from common-output-format.

tags:
  - quality
  - output-contract

inputs:
  prompt: "Use diagram-as-code-validation. Return Markdown with sections: ## Summary and ## Tool Results."
  context:
    target: SKILL.md

expected:
  output_contains:
    - "## Summary"
    - "## Tool Results"
  outcomes:
    - type: task_completed
