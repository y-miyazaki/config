# Usage: This workflow requires a GitHub App for auto-committing checksum updates.
# 1. Create a GitHub App with "Contents: Read & Write" repository permission.
# 2. Install the App on the target repository (or organization).
# 3. Create the following secrets (repository or environment):
#    - BOT_APP_ID: The GitHub App's App ID
#    - BOT_APP_PRIVATE_KEY: The GitHub App's private key (PEM format)
name: reusable-ci-aqua-update-checksum

on:
  workflow_call:
    inputs:
      aqua_version:
        default: "v2.55.2"
        description: "aqua version to use"
        required: false
        type: string
      environment:
        description: "Target environment"
        required: true
        type: string
    secrets:
      BOT_APP_ID:
        description: "GitHub App ID for auto commit"
        required: true
      BOT_APP_PRIVATE_KEY:
        description: "GitHub App private key for auto commit"
        required: true

permissions: {}

jobs:
  update-checksum:
    if: ${{ github.actor != 'dependabot[bot]' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    environment: ${{ inputs.environment }}

    permissions:
      contents: read

    concurrency:
      group: aqua-update-checksum-${{ github.repository }}-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout Code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Install aqua
        uses: aquaproj/aqua-installer@5e54e5cee8a95ee2ce7c04cb993da6dfad13e59c # v3.1.2
        with:
          aqua_version: ${{ inputs.aqua_version }}

      - name: Fix aqua-checksums.json
        run: aqua upc -prune

      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@d72941d797fd3113feb6b93fd0dec494b13a2547 # v1.12.0
        with:
          app-id: ${{ secrets.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
          permission-contents: write

      - name: Configure git auth
        uses: ./.github/actions/git-auth
        with:
          operation: configure
          token: ${{ steps.app-token.outputs.token }}

      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@b863ae1933cb653a53c021fe36dbb774e1fb9403 # v5.2.0
        with:
          commit_message: "chore(ci): update aqua checksums"
          commit_user_name: "repo-maintenance-bot[app]"
          commit_user_email: "41898282+github-actions[bot]@users.noreply.github.com"
          branch: ${{ github.head_ref || github.ref_name }}
          file_pattern: aqua-checksums.json

      - name: Clear git auth
        if: always()
        uses: ./.github/actions/git-auth
        with:
          operation: clear
          token: ${{ steps.app-token.outputs.token }}

      - name: Summary
        if: always()
        uses: ./.github/actions/summary
        with:
          params: |
            aqua_version: ${{ inputs.aqua_version }}
            environment: ${{ inputs.environment }}
          status: "${{ job.status }}"
          title: "Aqua Update Checksum"
