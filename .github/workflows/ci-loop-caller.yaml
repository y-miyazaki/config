# Reusable workflow: Loop caller orchestration (branch/PR)
# Shared detect → execute (matrix) → record-skip job graph for on-loop-*.yaml callers.
# Design doc: docs/explanation/loop-engineering/loop-caller-reusable-design.md
# Inputs reference: docs/explanation/loop-engineering/workflows/loop-caller-inputs-reference.md
# Token resolution: per-job loop-resolve-push-token (App → GH_TOKEN → job GITHUB_TOKEN).
# Do not fan out resolved tokens via job outputs — see:
#   docs/explanation/loop-engineering/loop-caller-reusable-design.md#github-token-resolution
name: ci-loop-caller

on:
  workflow_call:
    inputs:
      ack_trigger_comment:
        default: false
        description: "After detect proceeds, ACK the triggering comment with an eyes reaction (pr-revise)"
        required: false
        type: boolean
      additional_commit_paths:
        default: ""
        description: "Extra paths included in finalize commit (e.g. ledger file)"
        required: false
        type: string
      agent_checker_instructions:
        description: "Domain-specific APPROVE/REJECT rubric"
        required: true
        type: string
      agent_checker_max_turns:
        description: "Maximum checker agent turns per verification"
        required: true
        type: number
      agent_checker_model:
        description: "Checker model ID"
        required: true
        type: string
      agent_checker_skill_name:
        default: loop-verifier
        description: "Checker skill slash-loaded by loop-execute (distinct from agent_maker_skill_name)"
        required: false
        type: string
      agent_loop_max_attempts:
        description: "Maximum Agent→Verify retry cycles"
        required: true
        type: number
      agent_maker_instructions:
        default: ""
        description: "Domain-specific maker instructions"
        required: false
        type: string
      agent_maker_max_turns:
        description: "Maximum maker agent turns per loop attempt"
        required: true
        type: number
      agent_maker_model:
        description: "Maker model ID"
        required: true
        type: string
      agent_maker_skill_name:
        description: "Skill package name"
        required: true
        type: string
      allowlist:
        description: "Comma-separated globs the maker may modify"
        required: true
        type: string
      branch_match:
        default: ""
        description: "Comma-separated integration branch patterns to watch (empty = branch_state only)"
        required: false
        type: string
      branch_match_mode:
        default: glob
        description: "How to interpret branch_match: list, glob, or regex"
        required: false
        type: string
      branch_state:
        description: "Branch for .loop/* persistence, state migration, and watch fallback when branch_match is empty"
        required: true
        type: string
      budget_file:
        default: .loop/loop-budget.json
        description: "Path to loop budget JSON"
        required: false
        type: string
      budget_max_runs_per_day:
        description: "Daily run cap keyed by loop_name"
        required: false
        type: number
      budget_max_tokens_per_day:
        description: "Daily aggregated token cap"
        required: false
        type: number
      delivery:
        default: open_pr
        description: "Platform delivery after APPROVE: log | issue | notion | open_pr | none"
        required: false
        type: string
      denylist:
        default: ""
        description: "Comma-separated globs the maker must not touch"
        required: false
        type: string
      detect_domain_env_json:
        default: "{}"
        description: "JSON object of domain detect script environment variables"
        required: false
        type: string
      detect_script:
        description: "Path to domain detect_*.sh script"
        required: true
        type: string
      domain_persistence_script:
        default: ""
        description: "Bash script for loop-finalize domain persistence"
        required: false
        type: string
      engine:
        description: "AI engine: claude | copilot | codex | cursor"
        required: true
        type: string
      environment:
        default: default
        description: "GitHub environment for maintenance bot secrets"
        required: false
        type: string
      git_landing_integration:
        default: open_pr
        description: "Advanced git landing when delivery is open_pr: open_pr | push (integration targets)"
        required: false
        type: string
      git_landing_pull_request:
        default: open_pr
        description: "Advanced git landing when delivery is open_pr: open_pr | push_head (pull_request targets)"
        required: false
        type: string
      infer_files_pattern:
        default: ""
        description: "Extended regex to infer file paths from checker text"
        required: false
        type: string
      level:
        default: L2
        description: "Autonomy: L1 | L2 | L3"
        required: false
        type: string
      loop_name:
        description: "Loop identifier for state, budget, and concurrency"
        required: true
        type: string
      max_targets_per_schedule:
        default: 3
        description: "Max targets per cron tick after filters"
        required: false
        type: number
      may_edit:
        description: "Agent worktree edit gate: true | false"
        required: true
        type: boolean
      no_changes_verdict:
        default: REJECT
        description: "APPROVE | REJECT when maker produces no file diff"
        required: false
        type: string
      pr_body:
        default: ""
        description: "Static markdown prefix for finalize PR body"
        required: false
        type: string
      pr_draft:
        default: false
        description: "Create open_pr finalize PR as draft when true"
        required: false
        type: boolean
      pr_enabled:
        default: false
        description: "Watch open PR heads for detect (pr_enabled)"
        required: false
        type: boolean
      pr_exclude:
        default: fork,draft,label:no-loop
        description: "PR exclusion tokens"
        required: false
        type: string
      pr_include_bots:
        default: ""
        description: "Comma-separated bot logins to include when scanning PRs"
        required: false
        type: string
      pr_title:
        default: ""
        description: "PR title when finalize strategy is open_pr"
        required: false
        type: string
      priority:
        default: integration,pull_request
        description: "Target mode priority order (comma-separated)"
        required: false
        type: string
      run_log_file:
        default: .loop/loop-run-log.md
        description: "JSONL run log path for budget aggregation"
        required: false
        type: string
      scoped_pr_number:
        description: "When set, detect watches only this open PR number (skip integration fan-out); 0 = unset"
        required: false
        type: number
      state_file:
        default: ""
        description: "Override state JSON path (default .loop/state-<loop_name>.json)"
        required: false
        type: string
      write_target:
        description: "Agent artifact when may_edit is true: fix | report"
        required: true
        type: string
    secrets:
      AGENT_TOKEN:
        description: "Authentication token for the selected engine"
        required: true
      BOT_APP_CLIENT_ID:
        description: "GitHub App client ID for ruleset-bypass .loop/* pushes"
        required: false
      BOT_APP_PRIVATE_KEY:
        description: "GitHub App private key for ruleset-bypass .loop/* pushes"
        required: false
      GH_TOKEN:
        description: "Optional explicit GitHub token for resolution (defaults to job GITHUB_TOKEN when empty)"
        required: false

defaults:
  run:
    shell: bash

jobs:
  detect:
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      actions: write
      contents: read
      pull-requests: read
    outputs:
      additional_commit_paths: ${{ inputs.additional_commit_paths }}
      agent_checker_instructions: ${{ steps.detect.outputs.agent_checker_instructions }}
      agent_checker_max_turns: ${{ steps.detect.outputs.agent_checker_max_turns }}
      agent_checker_model: ${{ steps.detect.outputs.agent_checker_model }}
      agent_loop_max_attempts: ${{ steps.detect.outputs.agent_loop_max_attempts }}
      agent_maker_max_turns: ${{ steps.detect.outputs.agent_maker_max_turns }}
      agent_maker_model: ${{ steps.detect.outputs.agent_maker_model }}
      agent_maker_skill_name: ${{ steps.detect.outputs.agent_maker_skill_name }}
      allowlist: ${{ steps.detect.outputs.allowlist }}
      branch_state: ${{ inputs.branch_state }}
      delivery: ${{ steps.detect.outputs.delivery }}
      denylist: ${{ inputs.denylist }}
      domain_persistence_script: ${{ inputs.domain_persistence_script }}
      engine: ${{ steps.detect.outputs.engine }}
      handoff_artifact_name: ${{ steps.detect.outputs.handoff_artifact_name }}
      infer_files_pattern: ${{ steps.detect.outputs.infer_files_pattern }}
      level: ${{ steps.detect.outputs.level }}
      loop_name: ${{ steps.detect.outputs.loop_name }}
      no_changes_verdict: ${{ steps.detect.outputs.no_changes_verdict }}
      pr_body: ${{ steps.detect.outputs.pr_body }}
      pr_title: ${{ steps.detect.outputs.pr_title }}
      run_started_at: ${{ steps.timing.outputs.started_at }}
      should_run: ${{ steps.detect.outputs.should_run }}
      skip_reason: ${{ steps.detect.outputs.skip_reason }}
      state_file: ${{ steps.detect.outputs.state_file }}
      state_push_branch: ${{ inputs.branch_state }}
      target_matrix: ${{ steps.detect.outputs.target_matrix }}
    steps:
      - name: Record Run Start
        id: timing
        run: echo "started_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Export Detect Domain Env
        env:
          DETECT_DOMAIN_ENV_JSON: ${{ inputs.detect_domain_env_json }}
        run: |
          set -euo pipefail
          if [[ -n "${DETECT_DOMAIN_ENV_JSON}" && "${DETECT_DOMAIN_ENV_JSON}" != "{}" ]]; then
            if ! echo "${DETECT_DOMAIN_ENV_JSON}" | jq -e 'type == "object"' >/dev/null; then
              echo "detect_domain_env_json must be a JSON object" >&2
              exit 1
            fi
            if ! echo "${DETECT_DOMAIN_ENV_JSON}" | jq -e               'to_entries | map(.key) | all(test("^[A-Z_][A-Z0-9_]*$"))'               >/dev/null; then
              bad_key="$(echo "${DETECT_DOMAIN_ENV_JSON}" | jq -r                 'to_entries[] | select(.key | test("^[A-Z_][A-Z0-9_]*$") | not) | .key'                 | head -1)"
              echo "detect_domain_env_json keys must match ^[A-Z_][A-Z0-9_]*$ (invalid: ${bad_key})" >&2
              exit 1
            fi
            if ! echo "${DETECT_DOMAIN_ENV_JSON}" | jq -e \
              '[to_entries[] | select(.value != "" and ((.value | tostring) | test("[\r\n]")))] | length == 0' \
              >/dev/null; then
              bad_key="$(echo "${DETECT_DOMAIN_ENV_JSON}" | jq -r \
                'to_entries[] | select(.value != "" and ((.value | tostring) | test("[\r\n]"))) | .key' \
                | head -1)"
              echo "detect_domain_env_json values must not contain newlines (key: ${bad_key})" >&2
              exit 1
            fi
            while IFS= read -r -d '' entry; do
              printf '%s\n' "${entry}" >> "${GITHUB_ENV}"
            done < <(echo "${DETECT_DOMAIN_ENV_JSON}" | jq -jr \
              'to_entries[] | select(.value != "") | "\(.key)=\(.value)\u0000"')
          fi

      - name: Resolve GitHub Token
        id: resolve_github_token
        uses: y-miyazaki/config/.github/actions/loop-resolve-push-token@dd88a86ff8d193d62334f631c5441ed2351d9dc6 # v1.8.97
        with:
          bot_app_client_id: ${{ secrets.BOT_APP_CLIENT_ID }}
          bot_app_private_key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
          github_token: ${{ secrets.GH_TOKEN }}

      - name: Detect Loop Work
        id: detect
        uses: y-miyazaki/config/.github/actions/loop-detect@dd88a86ff8d193d62334f631c5441ed2351d9dc6 # v1.8.97
        with:
          agent_checker_instructions: ${{ inputs.agent_checker_instructions }}
          agent_checker_max_turns: ${{ format('{0}', inputs.agent_checker_max_turns) }}
          agent_checker_model: ${{ inputs.agent_checker_model }}
          agent_loop_max_attempts: ${{ format('{0}', inputs.agent_loop_max_attempts) }}
          agent_maker_instructions: ${{ inputs.agent_maker_instructions }}
          agent_maker_max_turns: ${{ format('{0}', inputs.agent_maker_max_turns) }}
          agent_maker_model: ${{ inputs.agent_maker_model }}
          agent_maker_skill_name: ${{ inputs.agent_maker_skill_name }}
          allowlist: ${{ inputs.allowlist }}
          base_branch: ${{ inputs.branch_state }}
          budget_file: ${{ inputs.budget_file }}
          budget_max_runs_per_day: ${{ inputs.budget_max_runs_per_day != null && format('{0}', inputs.budget_max_runs_per_day) || '' }}
          budget_max_tokens_per_day: ${{ inputs.budget_max_tokens_per_day != null && format('{0}', inputs.budget_max_tokens_per_day) || '' }}
          delivery: ${{ inputs.delivery }}
          detect_script: ${{ inputs.detect_script }}
          engine: ${{ inputs.engine }}
          git_landing_integration: ${{ inputs.git_landing_integration }}
          git_landing_pull_request: ${{ inputs.git_landing_pull_request }}
          github_token: ${{ steps.resolve_github_token.outputs.github_token }}
          infer_files_pattern: ${{ inputs.infer_files_pattern }}
          level: ${{ inputs.level }}
          loop_branch_match: ${{ inputs.branch_match_mode }}
          loop_integration_branches: ${{ inputs.branch_match }}
          loop_max_targets_per_schedule: ${{ format('{0}', inputs.max_targets_per_schedule) }}
          loop_name: ${{ inputs.loop_name }}
          loop_pr_enabled: ${{ inputs.pr_enabled && 'true' || 'false' }}
          loop_pr_exclude: ${{ inputs.pr_exclude }}
          loop_pr_include_bots: ${{ inputs.pr_include_bots }}
          loop_priority: ${{ inputs.priority }}
          loop_scoped_pr_number: ${{ inputs.scoped_pr_number != 0 && format('{0}', inputs.scoped_pr_number) || '' }}
          loop_state_push_branch: ${{ inputs.branch_state }}
          may_edit: ${{ inputs.may_edit == true && 'true' || inputs.may_edit == false && 'false' || '' }}
          no_changes_verdict: ${{ inputs.no_changes_verdict }}
          pr_body: ${{ inputs.pr_body }}
          pr_title: ${{ inputs.pr_title }}
          run_log_file: ${{ inputs.run_log_file }}
          state_file: ${{ inputs.state_file }}
          write_target: ${{ inputs.write_target }}

  ack-trigger:
    if: >-
      inputs.ack_trigger_comment &&
      needs.detect.outputs.should_run == 'true' &&
      (github.event_name == 'issue_comment' ||
      github.event_name == 'pull_request_review_comment')
    needs: detect
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-24.04
    timeout-minutes: 2
    permissions:
      actions: read
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: Resolve GitHub Token
        id: resolve_github_token
        uses: y-miyazaki/config/.github/actions/loop-resolve-push-token@dd88a86ff8d193d62334f631c5441ed2351d9dc6 # v1.8.97
        with:
          bot_app_client_id: ${{ secrets.BOT_APP_CLIENT_ID }}
          bot_app_private_key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
          github_token: ${{ secrets.GH_TOKEN }}

      - name: Download Loop Handoff
        if: needs.detect.outputs.handoff_artifact_name != ''
        continue-on-error: true
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ needs.detect.outputs.handoff_artifact_name }}
          path: ${{ runner.temp }}/loop-handoff

      - name: Ack Gathered Comments
        continue-on-error: true
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_TOKEN: ${{ steps.resolve_github_token.outputs.github_token }}
          HANDOFF_DIR: ${{ runner.temp }}/loop-handoff
          REPOSITORY: ${{ github.repository }}
          TRIGGER_COMMENT_ID: ${{ github.event.comment.id }}
        run: |
          set -euo pipefail
          ack_one() {
            local comment_id="$1"
            local source="$2"
            local endpoint
            case "${source}" in
              issue_comment)
                endpoint="repos/${REPOSITORY}/issues/comments/${comment_id}/reactions"
                ;;
              pull_request_review_comment)
                endpoint="repos/${REPOSITORY}/pulls/comments/${comment_id}/reactions"
                ;;
              *)
                echo "::warning::Skipping ACK for comment ${comment_id} (unknown source: ${source})"
                return 0
                ;;
            esac
            if gh api --method POST "${endpoint}" -f content='eyes' >/dev/null; then
              echo "::notice title=trigger-ack::Added eyes reaction on comment ${comment_id}"
            else
              echo "::warning::Failed to ACK comment ${comment_id} (${source})"
            fi
          }
          acked=0
          if [[ -d "${HANDOFF_DIR}/payloads" ]] && compgen -G "${HANDOFF_DIR}/payloads/*.json" >/dev/null; then
            while IFS= read -r item; do
              [[ -z "${item}" || "${item}" == "null" ]] && continue
              cid="$(jq -r 'if .comment_id then (.comment_id | tostring) else empty end' <<< "${item}")"
              src="$(jq -r '.source // empty' <<< "${item}")"
              if [[ -z "${cid}" ]]; then
                continue
              fi
              if [[ -z "${src}" ]]; then
                src="${GITHUB_EVENT_NAME}"
              fi
              ack_one "${cid}" "${src}"
              acked=$((acked + 1))
            done < <(jq -sc '
              [.[].result.result.comments // [] | .[]]
              | map({comment_id, source})
              | unique_by(.comment_id)
              | .[]
            ' "${HANDOFF_DIR}"/payloads/*.json)
          fi
          if [[ "${acked}" -eq 0 && -n "${TRIGGER_COMMENT_ID}" && -n "${REPOSITORY}" ]]; then
            ack_one "${TRIGGER_COMMENT_ID}" "${GITHUB_EVENT_NAME}"
          fi

  execute:
    if: >-
      always() &&
      needs.detect.result == 'success' &&
      needs.detect.outputs.should_run == 'true' &&
      (needs.ack-trigger.result == 'success' ||
      needs.ack-trigger.result == 'skipped')
    needs:
      - ack-trigger
      - detect
    permissions:
      actions: read
      contents: write
      copilot-requests: write # zizmor: ignore[excessive-permissions]
      issues: write
      pull-requests: write
    strategy:
      fail-fast: false
      matrix:
        target: ${{ fromJson(needs.detect.outputs.target_matrix) }}
    uses: y-miyazaki/config/.github/workflows/ci-loop-agent.yaml@dd88a86ff8d193d62334f631c5441ed2351d9dc6 # v1.8.97
    with:
      additional_commit_paths: ${{ needs.detect.outputs.additional_commit_paths }}
      agent_checker_instructions: ${{ needs.detect.outputs.agent_checker_instructions }}
      agent_checker_max_turns: ${{ fromJSON(needs.detect.outputs.agent_checker_max_turns) }}
      agent_checker_model: ${{ needs.detect.outputs.agent_checker_model }}
      agent_checker_skill_name: ${{ inputs.agent_checker_skill_name }}
      agent_loop_max_attempts: ${{ fromJSON(needs.detect.outputs.agent_loop_max_attempts) }}
      agent_maker_max_turns: ${{ fromJSON(needs.detect.outputs.agent_maker_max_turns) }}
      agent_maker_model: ${{ needs.detect.outputs.agent_maker_model }}
      agent_maker_skill_name: ${{ needs.detect.outputs.agent_maker_skill_name }}
      allowlist: ${{ needs.detect.outputs.allowlist }}
      auto_merge: ${{ (needs.detect.outputs.delivery == 'open_pr' && needs.detect.outputs.level == 'L3' && matrix.target.target_json.finalize == 'open_pr') && 'true' || 'false' }}
      base_branch: ${{ matrix.target.target_json.mode == 'pull_request' && matrix.target.target_json.base.branch || matrix.target.target_json.to.branch }}
      current_sha: ${{ matrix.target.target_json.from.ref }}
      denylist: ${{ needs.detect.outputs.denylist }}
      detect_result_json: "{}"
      domain_persistence_script: ${{ needs.detect.outputs.domain_persistence_script }}
      engine: ${{ needs.detect.outputs.engine }}
      environment: ${{ inputs.environment }}
      finalize_enabled: ${{ needs.detect.outputs.delivery == 'open_pr' }}
      handoff_artifact_name: ${{ needs.detect.outputs.handoff_artifact_name }}
      handoff_key: ${{ matrix.target.handoff_key }}
      infer_files_pattern: ${{ needs.detect.outputs.infer_files_pattern }}
      level: ${{ needs.detect.outputs.level }}
      loop_name: ${{ needs.detect.outputs.loop_name }}
      no_changes_verdict: ${{ needs.detect.outputs.no_changes_verdict }}
      pr_body: ${{ needs.detect.outputs.pr_body }}
      pr_draft: ${{ inputs.pr_draft }}
      pr_title: ${{ needs.detect.outputs.pr_title }}
      prompt_text: ${{ matrix.target.prompt }}
      run_log_file: ${{ inputs.run_log_file }}
      run_started_at: ${{ needs.detect.outputs.run_started_at }}
      skip_reason: ${{ needs.detect.outputs.skip_reason }}
      state_file: ${{ needs.detect.outputs.state_file }}
      state_push_branch: ${{ needs.detect.outputs.state_push_branch }}
      target_json: ${{ toJson(matrix.target.target_json) }}
      verifier_context: ${{ matrix.target.verifier_context }}
    secrets:
      AGENT_TOKEN: ${{ secrets.AGENT_TOKEN }}
      BOT_APP_CLIENT_ID: ${{ secrets.BOT_APP_CLIENT_ID }}
      BOT_APP_PRIVATE_KEY: ${{ secrets.BOT_APP_PRIVATE_KEY }}
      GH_TOKEN: ${{ secrets.GH_TOKEN }}

  record-skip:
    if: >-
      needs.detect.result == 'success' &&
      needs.detect.outputs.should_run == 'false' &&
      (needs.detect.outputs.skip_reason == 'circuit_breaker' ||
      needs.detect.outputs.skip_reason == 'budget')
    needs: detect
    environment: ${{ inputs.environment }}
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Resolve GitHub Token
        id: resolve_github_token
        uses: y-miyazaki/config/.github/actions/loop-resolve-push-token@dd88a86ff8d193d62334f631c5441ed2351d9dc6 # v1.8.97
        with:
          bot_app_client_id: ${{ secrets.BOT_APP_CLIENT_ID }}
          bot_app_private_key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
          github_token: ${{ secrets.GH_TOKEN }}

      - name: Append Skip Run Log
        uses: y-miyazaki/config/.github/actions/loop-run-log@dd88a86ff8d193d62334f631c5441ed2351d9dc6 # v1.8.97
        with:
          base_branch: ${{ needs.detect.outputs.branch_state }}
          github_token: ${{ steps.resolve_github_token.outputs.github_token }}
          loop_name: ${{ needs.detect.outputs.loop_name }}
          outcome: skipped
          run_log_file: ${{ inputs.run_log_file }}
          run_started_at: ${{ needs.detect.outputs.run_started_at }}
          skip_reason: ${{ needs.detect.outputs.skip_reason }}
