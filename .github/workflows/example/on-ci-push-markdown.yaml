# CI workflow: Markdown lint on push/PR
name: on-ci-push-markdown

on:
    push:
        branches:
            - "main"
            - "master"
            - "hotfix/**"
            - "release/**"
            - "develop"
            - "feature/**"
        paths:
            - "**/*.md"
            - "docs/**/*.md"
            - ".markdownlint-cli2.yaml"
    pull_request:
        branches:
            - "main"
            - "master"
            - "hotfix/**"
            - "release/**"
            - "develop"
            - "feature/**"
        paths:
            - "**/*.md"
            - "docs/**/*.md"
            - ".markdownlint-cli2.yaml"

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

permissions:
    contents: read
    pull-requests: write

jobs:
<<<<<<< HEAD
    markdown-ci:
        uses: y-miyazaki/config/.github/workflows/ci-markdown.yaml@86080c96bb7a03745719a81205b693bff6906e4c # v1.8.27
        permissions:
            contents: read
            pull-requests: write
        with:
            markdownlint_flags: "README*.md docs/**/*.md"
=======
  markdown-ci:
    uses: y-miyazaki/config/.github/workflows/ci-markdown.yaml@ce70870e6b5943030c3193a59972616f0d9b644c # v1.8.26
    permissions:
      contents: read
      pull-requests: write
    with:
      markdownlint_flags: "README*.md docs/**/*.md"
>>>>>>> 77ffacd2 (chore: pin all to release v1.8.27 (9834099))
