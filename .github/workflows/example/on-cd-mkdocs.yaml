# Deploy MkDocs documentation to GitHub Pages on push to main
name: on-cd-mkdocs

on:
  push:
    branches:
      - "main"
    paths:
      - "docs/**"
      - "mkdocs.yml"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  pages: write

jobs:
  build:
    permissions:
      contents: read
    uses: y-miyazaki/config/.github/workflows/cd-mkdocs.yaml@cd498a0b7d7d69468d24398fa39daa87c767f76a # v1.1.37
    with:
      artifact_name: mkdocs-site
      config_file: mkdocs.yml

  deploy:
    needs: build
    permissions:
      contents: read
      id-token: write
      pages: write
    uses: y-miyazaki/config/.github/workflows/cd-github-pages.yaml@cd498a0b7d7d69468d24398fa39daa87c767f76a # v1.1.37
    with:
      artifact_name: mkdocs-site
