# CI workflow: Shell script lint on push/PR
name: on-ci-push-shell-script

on:
    push:
        branches:
            - "main"
            - "master"
            - "hotfix/**"
            - "release/**"
            - "develop"
            - "feature/**"
        paths:
            - "**/*.sh"
            - "**/*.bats"
    pull_request:
        branches:
            - "main"
            - "master"
            - "hotfix/**"
            - "release/**"
            - "develop"
            - "feature/**"
        paths:
            - "**/*.sh"
            - "**/*.bats"

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

permissions:
    checks: write
    contents: read
    pull-requests: write

jobs:
<<<<<<< HEAD
    shell-script-ci:
        uses: y-miyazaki/config/.github/workflows/ci-shell-script.yaml@86080c96bb7a03745719a81205b693bff6906e4c # v1.8.27
        with:
            script_path: "**/*.sh,**/*.bats"
        permissions:
            checks: write
            contents: read
            pull-requests: write
=======
  shell-script-ci:
    uses: y-miyazaki/config/.github/workflows/ci-shell-script.yaml@cf11d87d5638a708323bc96ccb307d1049db59e9 # v1.8.27
    with:
      script_path: "**/*.sh,**/*.bats"
    permissions:
      checks: write
      contents: read
      pull-requests: write
>>>>>>> 77ffacd2 (chore: pin all to release v1.8.27 (9834099))
